import { loadRuntimeConfig } from "@/lib/runtime/config";import { errorResponse } from "@/lib/runtime/errors";import { readVerifiedForecastOutcome } from "@/lib/runtime/outcome-store";export const runtime="nodejs";
export async function GET(_request:Request,context:{params:Promise<{outcomeId:string}>}):Promise<Response>{try{const {outcomeId}=await context.params;const config=loadRuntimeConfig(false);const value=await readVerifiedForecastOutcome(config.runtimeRoot,outcomeId);return Response.json({ok:true,...value},{headers:{"Cache-Control":"no-store"}});}catch(error){const failure=errorResponse(error);return Response.json(failure.body,{status:failure.status,headers:{"Cache-Control":"no-store"}});}}
