import { loadRuntimeConfig } from "@/lib/runtime/config";import { errorResponse,RuntimePublicError } from "@/lib/runtime/errors";import { readVerifiedMonitoringSummary } from "@/lib/runtime/outcome-store";export const runtime="nodejs";
export async function GET(request:Request):Promise<Response>{try{const deploymentId=new URL(request.url).searchParams.get("deploymentId")??"dhaka_south";if(deploymentId!=="dhaka_south")throw new RuntimePublicError("monitoring_deployment_not_found","validation","The monitoring deployment is unavailable.",404);const config=loadRuntimeConfig(false);const value=await readVerifiedMonitoringSummary(config.runtimeRoot,deploymentId);return Response.json({ok:true,...value},{headers:{"Cache-Control":"no-store"}});}catch(error){const failure=errorResponse(error);return Response.json(failure.body,{status:failure.status,headers:{"Cache-Control":"no-store"}});}}
