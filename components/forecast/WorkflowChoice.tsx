import QuickForecastOption from "./QuickForecastOption"; import DatasetAssessmentOption from "./DatasetAssessmentOption"; import type { WorkflowMode } from "@/lib/forecast-workflow-types";
export default function WorkflowChoice({ value, onChange }: { value: WorkflowMode | null; onChange: (value: WorkflowMode) => void }) { return <fieldset><legend className="text-lg font-semibold text-ink">Choose workflow</legend><p className="mt-1 text-sm text-ink-muted">Both paths require a future governed runtime connector.</p><div className="mt-4 grid gap-4 md:grid-cols-2">{(["quick_forecast","assess_dataset"] as const).map(mode => <label key={mode} className={`cursor-pointer rounded-xl border p-5 ${value === mode ? "border-accent bg-accent/10" : "border-border-subtle bg-surface"}`}><input type="radio" className="sr-only" name="workflow-mode" checked={value === mode} onChange={() => onChange(mode)} />{mode === "quick_forecast" ? <QuickForecastOption selected={value === mode} /> : <DatasetAssessmentOption selected={value === mode} />}</label>)}</div></fieldset>; }
