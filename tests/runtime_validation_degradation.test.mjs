import test from "node:test";import assert from "node:assert/strict";import{readFile}from"node:fs/promises";const read=path=>readFile(new URL(`../${path}`,import.meta.url),"utf8");
test("Validation page presents separate read-only evidence dimensions",async()=>{const page=await read("app/validation/page.tsx"),panel=await read("components/validation/ModelDegradationEvidenceSummary.tsx");assert.match(page,/ModelDegradationEvidenceSummary/);assert.match(panel,/Assessment-reference evidence/);assert.match(panel,/Monitoring-window evidence/);assert.match(panel,/window_size_not_governed/);assert.match(panel,/No lifecycle decision/);assert.doesNotMatch(panel,/method:\s*["']POST|x-dengueops-internal-monitoring-secret/);for(const label of ["healthy","unhealthy","degraded","unsafe","retain","promote","replace","rollback"])assert.doesNotMatch(panel,new RegExp(`label=["']${label}["']`,"i"))});
