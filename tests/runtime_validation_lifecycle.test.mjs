import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("validation lifecycle panel is current, candidate-aware, read-only, and fail-closed",async()=>{const s=await readFile(new URL("../components/validation/ModelLifecycleSummary.tsx",import.meta.url),"utf8");assert.match(s,/Current governed model/);assert.match(s,/Verified committed assignment/);assert.match(s,/Current model authority unavailable/);assert.match(s,/Historical benchmark evidence remains available/);assert.match(s,/Technical evidence/);assert.match(s,/Refresh evidence/);assert.match(s,/Return to model assignment/);assert.doesNotMatch(s,/modelId\s*!==\s*["']random_forest|Exact RF compatibility|Active model/);assert.doesNotMatch(s,/method:\s*["']POST|promote button|rollback button|retain button/i)});
test("validation page includes lifecycle authority",async()=>{const s=await readFile(new URL("../app/validation/page.tsx",import.meta.url),"utf8");assert.match(s,/ModelLifecycleSummary/)});
